import { styled } from "../../styles";

export const Header = styled('header', {
  display: 'flex',
  justifyContent: 'space-between',

  svg: {
    width: '$6',
    height: '$6',
    cursor: 'pointer',
    color: '$gray200',
  }
})
